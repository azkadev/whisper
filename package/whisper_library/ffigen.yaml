# Run with `dart run ffigen --config ffigen.yaml`.
name: WhisperLibraryBindings
description: |
  Bindings for `src/whisper_library.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/whisper_library_bindings_generated.dart'
headers:
  entry-points:
    - 'src/whisper_library.h'
  include-directives:
    - 'src/whisper_library.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
